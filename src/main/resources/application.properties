# Server port
server.port=8080

# Jersey servlet mapping - all JAX-RS endpoints served under /api/*
spring.jersey.application-path=/api

# Enable WADL generation (disabled by default in Jersey 3)
spring.jersey.init.jersey.config.server.wadl.disableWadl=false